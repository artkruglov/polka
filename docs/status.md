# Состояние

Обновлено 24.09.2026. Статусы значат следующее: **работает** — есть в коде, покрыто тестами и доступно на polochka.app; **в коде** — реализовано и покрыто тестами, но на hosted-установке выключено или не проверено; **нет** — не реализовано.

| Область | Статус | Примечание |
|---|---|---|
| Вход по паролю, аккаунты от оператора | работает | Логины с паролем создаёт только оператор; открытая регистрация — по коду из письма (строка ниже) |
| Вход и регистрация по коду из письма | работает | Нужен SMTP (`MAIL_MODE=smtp`). Режим «по приглашению» (`EMAIL_SIGNUP=invite`): код только для привязанных аккаунтов и адресов из списка. На polochka.app регистрация открыта (`EMAIL_SIGNUP=open`) с потолком 50 новых полок в сутки и 3 с одного IP; письма — Yandex Cloud Postbox |
| Полка: папки, поиск, переименование, перенос | работает | |
| Версии: неизменяемые, SHA-256, CAS | работает | |
| Сравнение версий (построчный diff исходника) | работает | Во вкладке «Версии»: две версии, добавленные и удалённые строки, неизменённые свёрнуты. Читает те же маршруты владельца, что «Скачать оригинал»; у пакета — главная страница и список изменённых файлов. Считается в Web Worker: не больше 50 000 строк на сторону, дальше 2 500 отличий — замена целиком, показ до 2 000 изменённых строк. Изображения и двоичные файлы не сравниваются. |
| Превью ссылок (Open Graph) | работает | Сервер добавляет `og:*` и `twitter:*` в оболочку приложения, адреса из `APP_ORIGIN`. `/s` — одна общая карточка «Полка — вам отправили страницу»: токен в `#`, краулер его не видит, и название работы в превью не попадает. Остальные страницы — карточка Полки. Картинки 1200×630: `docs/design/og/*.svg` → `node scripts/render-og-images.mjs`. В Telegram/Slack ещё не проверено |
| Корзина и восстановление | работает | В интерфейсе и через MCP (`manage`) |
| Загрузка файла: HTML, текст, PNG/JPEG/WebP до 5 МБ | работает | |
| «Вставить код» (HTML/JSX) | работает | |
| Ссылки: срок 1/7/30 дней, отзыв, жалобы | работает | Ссылка привязана к версии |
| Статичный просмотр HTML в песочнице | работает | CSP без скриптов и сети |
| Статичный просмотр с домена просмотра, внешние ссылки через «Вы уходите с Полки» | работает | С настроенным viewer страница отдаётся только с него (`/static/:token`), домен приложения пользовательский HTML не отдаёт; ссылки ведут на подписанную страницу `/away`. |
| Интерактивный просмотр на отдельном домене | работает | `HTML_LIVE_MODE=production`, polochka.page |
| React runtime `react-runtime-v1` | работает | 10 библиотек и Tailwind v4, без сети |
| MCP для Claude Code и Codex | работает | Одной командой через OAuth: Codex CLI 0.153.4 проверен на polochka.app 23.09.2026 (вход и вызов `polka_context`); Claude Code — команда добавлена, вход не проверен. По токену — для остальных клиентов |
| Коннектор Claude.ai / ChatGPT (OAuth 2.1, `polka_publish`) | работает | Покрыт интеграционными тестами. Claude.ai: сохранение и ссылка проверены 22.09.2026; отзыв подключения и ChatGPT ещё не проверены ([приёмка](MCP_CONNECTOR.md#приёмка-после-выкладки)) |
| HTTP API `POST /api/v1/publish` и CLI | работает | Копия CLI из репозитория требует `POLKA_ENDPOINT` или `--endpoint` |
| Обнаружение агентом: `/llms.txt`, `/openapi.json`, `/.well-known/agent-skills`, скилл `polka` | работает | [connect-agents](connect-agents.md#агенту-без-подсказок-llmstxt-openapi-скилл). `npx skills add artkruglov/polka` заработает после попадания `skills/polka` в `main` на GitHub; адреса на polochka.app — после выкладки |
| Библиотеки шаблонов: роли, приглашения, журнал | работает | Приглашение пересылается вручную (письма нет) |
| «Интересное»: 12 интерактивных материалов Редакции | работает | Каталог курирует оператор; авторских публикаций нет |
| Импорт по URL (standalone HTML) | в коде | `URL_IMPORT_ENABLED`, выключен по умолчанию и на polochka.app |
| Импорт ссылок Claude/ChatGPT | нет | Сервер не может их забрать, см. [FAQ](faq.md#почему-нельзя-вставить-ссылку-на-артефакт-claude-или-chatgpt) |
| Удаление аккаунта | в коде | Отзыв доступа в коде (`ACCOUNT_DELETION_ENABLED`, выключено по умолчанию, для hosted не принято). Очистка данных — отдельный purge worker, не принят и не подключён к установке |
| Лицензия: AGPL-3.0 и коммерческая | работает | С версии 0.1.0 код под [AGPL-3.0](../LICENSE), до v0.1.0-rc.5 включительно — Apache-2.0. [Коммерческая лицензия](../COMMERCIAL.md) по договору, вклад — по [CLA](../CLA.md). Ссылка на исходный код — в подвале каждой страницы, у получателя, в `/llms.txt` и `/connect`; адрес задаёт `SOURCE_URL` (по умолчанию GitHub). Варианты использования — `/pricing`, страница «Для компаний» с заявкой — `/enterprise`. |
| Страница «Для компаний» и заявка | работает | `/enterprise`: ценность для компаний, варианты развёртывания, вопросы и форма заявки. `POST /api/enterprise-requests` (Origin, 5 в час с IP, honeypot) сохраняет заявку в `enterprise_requests` (миграция 033, удаляется через год) и шлёт письмо на `OPERATOR_EMAIL`. SSO и автоматическая проверка содержимого на странице помечены «в разработке». |
| Политика и соглашение | работает | Страницы `/privacy` и `/terms` из [docs/legal](legal/privacy.md). Заполнены и вычитаны по 152-ФЗ, 149-ФЗ и ГК ([вычитка](reviews/2026-09-23-legal/README.md)); юрист не подписал; за оператором — уведомление РКН, почта вне Gmail, адрес для обращений, runbook удаления |
| Резервное копирование | работает | Ежесуточный `pg_dump` и versioned-бакет. Восстановление проверено синтетическим drill, на отдельной VM ещё нет |
| Мониторинг и алерты | частично | Статус оператора `/api/ops/status` (БД, обслуживание, свежесть бэкапа, диск) и скрипт внешней проверки. Расписания и алертов нет |
| Вход через Яндекс ID, VK ID и свой OIDC | в коде | [SIGN_IN_PROVIDERS](specs/SIGN_IN_PROVIDERS.md). Миграция 032 (`account_identities`). Кнопки видны, когда задан клиент поставщика; привязка и отвязка в «Способах входа». На polochka.app приложения у Яндекса и VK ещё не зарегистрированы |
| Новые полки по коду — только на российских почтовых доменах | работает | `EMAIL_SIGNUP_DOMAINS=ru-only` (по умолчанию в `deploy/hosted`), существующие аккаунты входят как раньше (`EMAIL_LOGIN_DOMAINS=any`) |
| Доступ компании по домену / своему IdP | в коде | `ORG_DOMAINS`, `OIDC_ORG_LIBRARY`: вход через поставщика с подтверждённой почтой домена добавляет в библиотеку шаблонов (reader/curator). Настраивает оператор; самостоятельной настройки администратором библиотеки нет |
| Командные аккаунты, SCIM | нет | |
| Модерация: жалобы, закрытие ссылки, блокировка аккаунта | работает | Скрипты оператора `scripts/moderation.ts` ([hosted](../deploy/hosted/README.md#модерация)), без веб-интерфейса. На polochka.app ещё не запускались |
| Защита от злоупотреблений: доверие к аккаунту, лимиты новых аккаунтов, проверка ссылок, автопауза по жалобам | работает | [ABUSE_PROTECTION](specs/ABUSE_PROTECTION.md), пункты 1–7. Миграция 029. `SHARE_MODERATION` (в коде по умолчанию `flagged`, в `compose.yml` — `auto`), новый аккаунт — до 5 открытых ссылок по 7 дней, 3 разных жалобщика за 7 дней ставят ссылку на паузу. Получатель ожидающей ссылки видит экран «на проверке», над обычной страницей — предупреждение и «Пожаловаться». На polochka.app ещё не включено |
| Письма оператору с действиями в один клик | работает | `OPERATOR_EMAIL` + `MAIL_MODE=smtp`: письмо о каждой ожидающей ссылке, подозрительной странице, жалобе и паузе; кнопки ведут на `/moderation#…` (подпись HMAC, 7 дней, открытие ничего не меняет, действие — POST). Без адреса — скрипты `moderation:queue`/`approve`/`unpause`/`trust`. С реальной почтой ещё не проверено |
| Фильтр запрещённого содержимого и автоматическая модерация | работает | [CONTENT_FILTER](specs/CONTENT_FILTER.md). Миграция 031. Правила по 14 категориям (списки в `apps/server/content-filter/lists`, линейный проход, тесты точности на честных текстах), сигналы вредоносного кода, две модели после сохранения (Yandex AI Studio во всех ролях, NeuralDeep — необязательный вариант до поручения на обработку; у каждой роли своя точка, лимит запросов, 429 → запасная, фиксированный тариф) (блокировка только при согласии двух, CSAM модели — скрыть до проверки), `SHARE_MODERATION=auto` вместо ручного одобрения (ссылка, ждущая модель, открывается сама после её чистого ответа; `moderation.ts recheck`), состояние `blocked` с изоляцией и удалением по сроку категории, стоп-лист SHA-256, журнал `moderation_events` (3 года), `takedown`/`legal-hold`/`handed-over`, антиспам. Каталог «Интересное» проверен правилами: 15 из 15 чисто. С реальными моделями и почтой ещё не проверено; на polochka.app не включено |
| Признаки фишинга при сохранении | работает | Поля секретов + бренд или срочность, в том же проходе parse5, что и классификатор, и по строкам скриптов. Список — `apps/server/phishing-signals.ts`. Ссылку не удаляет: только задерживает или сообщает оператору |
| Заметки владельца вместо комментариев получателей | работает | `COMMENTS_MODE=owner-notes` (по умолчанию в `deploy/hosted`): пишет только владелец и его агент (`polka_note`), получатели читают; `off` выключает обсуждения |
| Комментарии и реакции на тексте работы | в коде | [COMMENTS](specs/COMMENTS.md), этап 3. Миграция 030. Обсуждение у каждой ссылки, лента и панель на телефоне, подсветка во фрейме (оверлей только по гранту оболочки; в статичном просмотре — nonce), письма с отключением, агентские `polka_comments`, `polka_resolve_comment`, правки патчем в `polka_revise` и `POST /api/v1/works/:id/edits`, модерация. Без домена просмотра (одна установка на одном домене) подсветки нет — только комментарии ко всей работе. На polochka.app ещё не включено |
| Публикации авторов, профили, веб-интерфейс модерации | нет | |
| Сверка изображений с базами хешей CSAM (PhotoDNA и т. п.) | нет | В России недоступны; см. «Ограничения» в [CONTENT_FILTER](specs/CONTENT_FILTER.md) |
| Расширение браузера, Telegram-бот | нет | См. [roadmap](roadmap.md) |

## Проверки

- `npm test` запускает интеграционные тесты на временных PostgreSQL и S3, `npm test -- --live` — наборы с включённым viewer. `npm run verify` запускает это вместе с проверкой прав ролей БД, лицензий зависимостей, секретов в истории и сборкой Docker-образов. Облачного CI нет: проверки запускаются локально перед push и деплоем.
- Быстрый запуск из [README](../README.md) проверен 22.09.2026 на чистом клоне: `npm ci` → `local:setup` → `infra:up` → `db:migrate` → `storage:bootstrap-local` → `account:create` → `build` → `dev`. После этого сработали `/api/health`, главная страница и вход созданным аккаунтом.
