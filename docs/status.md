# Состояние

Обновлено 22.09.2026. Статусы значат следующее: **работает** — есть в коде, покрыто тестами и доступно на polochka.app; **в коде** — реализовано и покрыто тестами, но на hosted-установке выключено или не проверено; **нет** — не реализовано.

| Область | Статус | Примечание |
|---|---|---|
| Вход по паролю, аккаунты от оператора | работает | Открытой регистрации на polochka.app нет |
| Вход и регистрация по коду из письма | в коде | Нужен SMTP (`MAIL_MODE=smtp`). Режим «по приглашению» (`EMAIL_SIGNUP=invite`): код только для привязанных аккаунтов и адресов из списка. На polochka.app выключено |
| Полка: папки, поиск, переименование, перенос | работает | |
| Версии: неизменяемые, SHA-256, CAS | работает | |
| Корзина и восстановление | работает | В интерфейсе и через MCP (`manage`) |
| Загрузка файла: HTML, текст, PNG/JPEG/WebP до 5 МБ | работает | |
| «Вставить код» (HTML/JSX) | работает | |
| Ссылки: срок 1/7/30 дней, отзыв, жалобы | работает | Ссылка привязана к версии |
| Статичный просмотр HTML в песочнице | работает | CSP без скриптов и сети |
| Интерактивный просмотр на отдельном домене | работает | `HTML_LIVE_MODE=production`, polochka.page |
| React runtime `react-runtime-v1` | работает | 10 библиотек и Tailwind v4, без сети |
| MCP по токену (Claude Code, Codex) | работает | Конфиг проверен на Codex CLI 0.153.4; Claude Code — по документации, не проверено |
| Коннектор Claude.ai / ChatGPT (OAuth 2.1, `polka_publish`) | работает | Покрыт интеграционными тестами. Claude.ai: сохранение и ссылка проверены 22.09.2026; отзыв подключения и ChatGPT ещё не проверены ([приёмка](MCP_CONNECTOR.md#приёмка-после-выкладки)) |
| HTTP API `POST /api/v1/publish` и CLI | работает | Копия CLI из репозитория требует `POLKA_ENDPOINT` или `--endpoint` |
| Библиотеки шаблонов: роли, приглашения, журнал | работает | Приглашение пересылается вручную (письма нет) |
| «Интересное»: 12 интерактивных материалов Редакции | работает | Каталог курирует оператор; авторских публикаций нет |
| Импорт по URL (standalone HTML) | в коде | `URL_IMPORT_ENABLED`, выключен по умолчанию и на polochka.app |
| Импорт ссылок Claude/ChatGPT | нет | Сервер не может их забрать, см. [FAQ](faq.md#почему-нельзя-вставить-ссылку-на-артефакт-claude-или-chatgpt) |
| Удаление аккаунта | в коде | Отзыв доступа в коде (`ACCOUNT_DELETION_ENABLED`, выключено по умолчанию, для hosted не принято). Очистка данных — отдельный purge worker, не принят и не подключён к установке |
| Резервное копирование | работает | Ежесуточный `pg_dump` и versioned-бакет. Восстановление проверено синтетическим drill, на отдельной VM ещё нет |
| Мониторинг и алерты | работает | Внешняя проверка каждые 15 минут из GitHub Actions: доступность, TLS обоих доменов, статус оператора (БД, обслуживание, свежесть бэкапа, диск). Алерт — письмо GitHub об упавшем запуске |
| Командные аккаунты, SSO/SCIM | нет | |
| Публикации авторов, профили, модерация | нет | |
| Расширение браузера, Telegram-бот | нет | См. [roadmap](roadmap.md) |

## Проверки

- `npm test` запускает интеграционные тесты на временных PostgreSQL и S3, `npm test -- --live` — наборы с включённым viewer. CI повторяет это на каждом pull request и дополнительно проверяет лицензии зависимостей, секреты в истории и собранный Docker-образ.
- Быстрый запуск из [README](../README.md) проверен 22.09.2026 на чистом клоне: `npm ci` → `local:setup` → `infra:up` → `db:migrate` → `storage:bootstrap-local` → `account:create` → `build` → `dev`. После этого сработали `/api/health`, главная страница и вход созданным аккаунтом.
