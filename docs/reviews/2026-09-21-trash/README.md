# Корзина: локальная приёмка 21.09.2026

Backend реализован Sol, UI — Luna, независимое ревью — Astra, браузерная приёмка — ведущий. Рабочая сборка перезапущена после приёмки исправлений.

## Доказательства

- Migration013: lifecycle CAS/retry/ABA, tenant-first locks, отзыв shares и grants всех версий, остановка незавершённых revisions/builds. Astra не нашла блокеров в scoped review.
- Sol: trash6/6, default76/76, live7/7, runtime3/3, MCP4/4; TypeScript passed. Synthetic restore schema13: trashStatePreserved и explicitTrashRestore true, 7/7 remap, остаточных тестовых DB/buckets нет.
- UI: исправлены старое содержимое при popstate, поздние результаты на другом экране/аккаунте, double-submit, отмена во время отправки и обработка refresh failure после409. Check/build прошли; changed-only review Astra принят.

## Реальный браузерный путь

Проверен только собственный искусственный материал `b5cfa9bd-fab1-4aa8-ad4f-19c135bdba04`, calculator-1.html, версия1. Исходная revision `43bf45d0-4bd2-4561-ba7e-e66caeb62cf3`.

1. Действовавшая до изменения backend ссылка открывала материал получателю.
2. «В корзину» показывает последствия и подтверждение. После отправки материал доступен в /trash; reload сохраняет результат.
3. Старая ссылка показывает недоступность.
4. «Версии и скачать» открывает материал в корзине: версия1 сохранена, preview и mutation controls отсутствуют, оригинал доступен.
5. Скачивание через UI создало calculator-1.html (1449 bytes). `cmp` с tests/fixtures/html-corpus/calculator-1.html: exit0. SHA256: `49621409412af657f57d067e5a7e88d0682770f0f8ef1152e55c781ff1f0580a`.
6. Back возвращает корректную корзину. «Восстановить» убирает материал из неё. Детальная страница снова показывает версию1, приватный доступ и обычные действия.
7. Reload прежней recipient ссылки после восстановления всё ещё показывает недоступность.

Тестовый материал оставлен восстановленным и приватным. Новую share ссылку не создавали. Временные браузерные вкладки закрываются после проверки.

## Границы

Гонки подтверждены scoped code review и backend tests; искусственная задержка UI-сети отдельно не воспроизводилась. Проверка локальная, не cloud/staging. Корзина сохраняет занимаемое место; она не закрывает R17 удаления аккаунта/окончательного удаления и retention. Инструменты MCP для trash/restore ещё требуют отдельной реализации scope manage.
