# Проверка обновления локальной установки

21.09.2026. Рабочая БД не изменялась, рабочий сервер не перезапускался.

- Dump schema16 восстановлен в случайную отдельную БД.
- Миграции 17–20 применились, повторное применение идемпотентно.
- Сравнены все прежние колонки всех 23 существующих таблиц: число строк и SHA256 отсортированной сериализации совпали до/после.
- Созданы account_purge_jobs, account_restore_suppressions, url_import_jobs.
- Из dump извлечены 6762 ссылки на версии объектов. 6368 скачаны и проверены по размеру/SHA256, 64 664 787 байт. Закрытая локальная копия и manifest находятся рядом с дампом в `.local/backups/pre-schema20-20260921/` и исключены из Git.
- 394 отсутствующих объекта полностью совпали с синтетическими fixtures: размер4, SHA256("note"), /agent-management/ + version (287), /mcp-test/ + test-version (107). Происхождение подтверждено tests/agent-management.test.ts и tests/mcp-transport.test.ts. Необъяснённых пропусков0. Они не удалялись и не подменялись.
- Scratch DB удалена. Секреты, названия пользовательских материалов и байты данных в этот отчёт не включены.

Ограничения: это подготовительный снимок, рабочий сервер продолжал работать; перед upgrade нужна свежая согласованная точка. Старые синтетические metadata-записи без S3 нельзя называть восстановленными файлами. Объекты проверены чтением из S3 и сохранены локально; восстановление этой копии в новый bucket ещё не выполнялось. Код старого запущенного процесса не архивирован. Полная эксплуатационная приёмка не заявлена.

Обнаружена отдельная причина загрязнения: часть npm test использует DATABASE_URL из рабочей .env и создаёт фиктивные ревизии без настоящих объектов. До следующего общего запуска изолировать весь suite, как уже изолированы URL-import runtime tests. Не чистить рабочую базу массово по шаблону имени.
