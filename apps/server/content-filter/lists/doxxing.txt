# Списки персональных данных (152-ФЗ; ст. 137, 272.1 УК РФ).
#
# Основной сигнал считают детекторы в scanner.ts: СНИЛС с верной
# контрольной суммой, номер карты по алгоритму Луна (кроме тестовых), серия и
# номер рядом со словом «паспорт», телефон рядом с адресом. Каждая разная
# запись — 3 очка, порог 9: одна-две записи (контакты, реквизиты перевода)
# не срабатывают, список — срабатывает. Здесь — слова «пробива» и сливов баз.
@threshold 9
@high 18
@pair probe target 4 4

0 пробив* | probe
0 пробить | probe
0 номеру | target
0 телефону | target
0 человека | target
0 фио | target
0 паспорту | target
0 адресу | target

4 слив* базы
4 слив* базу
4 база паспортов
4 базы паспортов
