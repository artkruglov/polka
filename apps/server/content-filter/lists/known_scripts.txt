# sha256 известных библиотечных скриптов (одна строка — один хеш): модель-ревьюер
# кода их не читает, правила — читают. Добавляйте хеши файлов, которые часто
# встраивают в страницы целиком (сборки React, Tailwind CDN и т. п.):
#   shasum -a 256 <файл>
# Интерактивная сборка Полки (react-runtime) в сохранённых версиях не лежит —
# она добавляется при просмотре, поэтому её здесь нет.
